public class Solution {
        long dataLen = data.length;
        DeviceType type = getBestDevice(dataLen);
        
        DeviceDriver driver = getNewDriver(type);
        
        boolean flag = driver.initializeDriver();
        if(!flag) return false;
        
        flag = driver.writeBytes(id, data);
        if(!flag) return false;
        
        return true;
}